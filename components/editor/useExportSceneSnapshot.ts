"use client"

import { useMemo } from "react"
import type { ExportSceneSnapshot } from "./ExportSceneSnapshot"

export function useExportSceneSnapshot({
  duration,
  materialPreset,
  colorA,
  colorB,
  fillKeyframes,
  materialSettings,
  materialKeyframes,
  roughness,
  metalness,
  reflectance,
  clearcoat,
  clearcoatRoughness,
  transmission,
  thickness,
  emissiveIntensity,
  extrusionDepth,
  bevelEnabled,
  bevelThickness,
  bevelSize,
  bevelSegments,
  layerSpacing,
  ambientIntensity,
  keyLightIntensity,
  rimLightIntensity,
  svgPathA,
  svgPathB,
  shapes,
  tracks,
  rotationOffset,
  rotationAxisKeyframes,
  objectScale,
  objectScaleAxes,
  moveOffset,
  moveKeyframes,
  keyLightPosition,
  keyLightPositionKeyframes,
}: ExportSceneSnapshot): ExportSceneSnapshot {
  return useMemo(
    () => ({
      duration,
      materialPreset,
      colorA,
      colorB,
      fillKeyframes,
      materialSettings,
      materialKeyframes,
      roughness,
      metalness,
      reflectance,
      clearcoat,
      clearcoatRoughness,
      transmission,
      thickness,
      emissiveIntensity,
      extrusionDepth,
      bevelEnabled,
      bevelThickness,
      bevelSize,
      bevelSegments,
      layerSpacing,
      ambientIntensity,
      keyLightIntensity,
      rimLightIntensity,
      svgPathA,
      svgPathB,
      shapes,
      tracks,
      rotationOffset,
      rotationAxisKeyframes,
      objectScale,
      objectScaleAxes,
      moveOffset,
      moveKeyframes,
      keyLightPosition,
      keyLightPositionKeyframes,
    }),
    [
      duration,
      materialPreset,
      colorA,
      colorB,
      fillKeyframes,
      materialSettings,
      materialKeyframes,
      roughness,
      metalness,
      reflectance,
      clearcoat,
      clearcoatRoughness,
      transmission,
      thickness,
      emissiveIntensity,
      extrusionDepth,
      bevelEnabled,
      bevelThickness,
      bevelSize,
      bevelSegments,
      layerSpacing,
      ambientIntensity,
      keyLightIntensity,
      rimLightIntensity,
      svgPathA,
      svgPathB,
      shapes,
      tracks,
      rotationOffset,
      rotationAxisKeyframes,
      objectScale,
      objectScaleAxes,
      moveOffset,
      moveKeyframes,
      keyLightPosition,
      keyLightPositionKeyframes,
    ]
  )
}
